import 'package:flutter/material.dart';

class DemoStateless extends StatelessWidget {
  const DemoStateless({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Halo')),
    );
  }
}
