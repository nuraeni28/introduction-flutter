import 'package:flutter/material.dart';

class DemoTextIcon extends StatelessWidget {
  const DemoTextIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      // body: Center(child: Icon(Icons.chair)),
      body: Text('Halo Makassar Coding', style: TextStyle(),),
    );
  }
}
